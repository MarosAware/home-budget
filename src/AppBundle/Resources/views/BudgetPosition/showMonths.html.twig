{% extends '@App/base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/BudgetPosition/style.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1 class="text-center">Wybierz miesiąc:</h1>
        </div>

        {% set months = ['Styczeń', 'Luty', 'Marzec', 'Kwiecień',
            'Maj', 'Czerwiec', 'Lipiec', 'Sierpień', 'Wrzesień', 'Październik', 'Listopad', 'Grudzień'] %}
        {% set k = 1 %}

        {% for i in 1..4 %}
            <div class="row">

                {% for j in range(1, 3)  %}

                    <div class="col-sm-4">
                        <div class="month-item">
                            <a href="{{ url('app_budgetposition_onemonth', {'year': year, 'monthId': k, 'month': months[k-1] }) }}">{{ months[k-1]}}</a>
                        </div>
                    </div>
                    {% set k = k + 1 %}
                {% endfor %}

            </div>
        {% endfor %}


    </div>
{% endblock %}

